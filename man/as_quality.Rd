% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapters.R
\name{as_quality}
\alias{as_quality}
\title{Build a retention indicator from the output of an existing cleaning tool}
\usage{
as_quality(
  x,
  column = ".summary",
  threshold = NULL,
  keep = c("below", "above"),
  true_level = NULL
)
}
\arguments{
\item{x}{A data frame, or a logical, numeric, character or factor vector.}

\item{column}{Column name to use when \code{x} is a data frame. Defaults to
\code{.summary}.}

\item{threshold}{Numeric cut-off, required when \code{x} is numeric.}

\item{keep}{\code{"below"} (default) retains records with values at or below
\code{threshold}, which is what you want for an uncertainty or error field;
\code{"above"} retains those at or above it, which is what you want for a
confidence or precision score.}

\item{true_level}{The level of a character or factor vector that denotes a
retained record.}
}
\value{
A logical vector, \code{TRUE} for retained records.
}
\description{
\code{\link[=audit]{audit()}} needs to know which records a filter kept. Cleaning tools express
that in different ways, and this function is the bridge. It exists so that
auditing a filter costs one line rather than a rewrite of your pipeline.
}
\details{
Three inputs are recognised.

\describe{
\item{A data frame}{The \code{.summary} column produced by \strong{CoordinateCleaner}
(Zizka et al., 2019) is used if present: \code{TRUE} means the record passed
every test. Any other column can be named through \code{column}.}
\item{A logical vector}{Returned unchanged, after validation.}
\item{A numeric vector}{Thresholded. This is the case for a continuous
coordinate-uncertainty field, e.g. GBIF's
\code{coordinateUncertaintyInMeters}: \code{keep = "below"} retains records at or
under \code{threshold}.}
\item{A character or factor vector}{Compared against \code{true_level}.}
}
}
\section{On sweeping the threshold}{

When quality is continuous, there is no single correct cut. Auditing at one
threshold answers a question you chose; auditing across a range of thresholds
answers whether the answer depends on that choice. Sweep it. If the shift
grows as the cut gets stricter, then "cleaning harder" is making the bias
worse, and there is no bias-free threshold to find.
}

\examples{
# A continuous coordinate-uncertainty field, as GBIF supplies it
unc <- c(10, 50, 120, 3000, NA, 80)
as_quality(unc, threshold = 100)

# The shape CoordinateCleaner returns
cc <- data.frame(x = 1:3, .summary = c(TRUE, FALSE, TRUE))
as_quality(cc)
}
\references{
Zizka, A. et al. (2019). CoordinateCleaner: standardized cleaning of
occurrence records from biological collection databases. \emph{Methods in Ecology
and Evolution}, 10(5), 744-751.
}
