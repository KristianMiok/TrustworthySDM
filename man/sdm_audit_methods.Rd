% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit-methods.R
\name{sdm_audit_methods}
\alias{sdm_audit_methods}
\alias{print.sdm_audit}
\alias{summary.sdm_audit}
\alias{print.summary.sdm_audit}
\alias{plot.sdm_audit}
\title{Print, summarise and plot an audit}
\usage{
\method{print}{sdm_audit}(x, ...)

\method{summary}{sdm_audit}(object, n_axes = 6L, ...)

\method{print}{summary.sdm_audit}(x, ...)

\method{plot}{sdm_audit}(x, n_axes = 10L, ...)
}
\arguments{
\item{x, object}{An \code{sdm_audit} object from \code{\link[=audit]{audit()}}.}

\item{...}{Ignored.}

\item{n_axes}{Number of environmental axes to show, ordered by \verb{|smd|}.}
}
\value{
\code{print()} and \code{plot()} return their input invisibly. \code{summary()}
returns an object of class \code{summary.sdm_audit}.
}
\description{
\code{print()} gives the headline numbers. \code{summary()} gives the reportable
verdict: the shifted axes, the multivariate test, the propensity
decomposition, the positivity limit, and what each of them licenses you to
do. \code{plot()} shows the two things worth looking at, which are the per-axis
shift and the separation of the propensity scores.
}
\details{
The verdict is one of:

\describe{
\item{\code{benign}}{No environmental signature of the filter. Filtering and
weighting will agree, and the filtering decision is not carrying your
result.}
\item{\code{structured}}{The filter moved the niche and positivity supports
undoing it. Fit both ends of the bracket (filtered, and IPW-weighted) and
report the spread.}
\item{\code{positivity-limited}}{The filter moved the niche, but reweighting is
carried by a handful of records. Global IPW is fragile here. Stratify, or
report the bracket without committing to either end.}
}
}
\examples{
d <- sim_occ(n = 400, seed = 1)
a <- audit(d,
  quality = "keep_precision", env = c("bio1", "bio12", "elev"),
  metadata = "source", spatial_block = "basin",
  control = audit_control(n_perm = 99, max_n = 200)
)
a
summary(a)
}
\seealso{
\code{\link[=audit]{audit()}}
}
