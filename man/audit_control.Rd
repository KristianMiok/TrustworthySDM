% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit_control}
\alias{audit_control}
\title{Tuning constants for \code{\link[=audit]{audit()}}}
\usage{
audit_control(
  smd_threshold = 0.1,
  auc_threshold = 0.6,
  alpha = 0.05,
  ess_threshold = 0.1,
  n_perm = 999L,
  max_n = 1500L,
  k = 5L,
  quadratic = TRUE,
  pca = FALSE,
  pca_var = 0.95,
  trim = c(0.01, 0.99),
  reference = c("all", "low")
)
}
\arguments{
\item{smd_threshold}{Absolute standardized mean difference above which an
environmental axis counts as shifted. The 0.1 convention comes from the
propensity-score balance literature (Austin, 2011).}

\item{auc_threshold}{Out-of-fold AUC of the environmental propensity model
above which retention counts as environmentally structured. 0.5 is
no structure; 0.6 is a deliberately low bar, because in a reliability audit
a false alarm is cheaper than false reassurance.}

\item{alpha}{Significance level for the energy-distance permutation test.}

\item{ess_threshold}{Effective sample size, as a fraction of the retained
records, below which inverse-propensity weighting is treated as
positivity-limited: the weights are then carried by a small number of
records and global reweighting is fragile.}

\item{n_perm}{Permutations for the energy test. The smallest attainable
p-value is \code{1 / (n_perm + 1)}; keep this well below \code{alpha} or the test
will report its own resolution floor rather than the evidence.}

\item{max_n}{Common subsample size for the energy test, which bounds the
\eqn{O(n^2)} pairwise-distance computation.}

\item{k}{Number of cross-validation folds.}

\item{quadratic}{Include squared environmental terms in the propensity model.
Keep this on: a filter that trims environmental outliers leaves the mean
untouched and is invisible to a linear propensity model.}

\item{pca}{Reduce the environmental block by PCA before fitting the
propensity model. Useful when predictors are numerous and collinear, but
conservative: it will tend to understate the environmental signal.}

\item{pca_var}{Proportion of variance retained when \code{pca = TRUE}.}

\item{trim}{Quantiles at which inverse-propensity weights are trimmed.}

\item{reference}{Reference sample for the energy test: \code{"all"} (the contrast
a model fitted on filtered data actually experiences) or \code{"low"} (the
disjoint high-versus-low contrast).}
}
\value{
A list of class \code{audit_control}.
}
\description{
The thresholds below are \strong{conventions, not truths}. They are the
conventional cut-offs from the covariate-balance and propensity literature,
and they are exposed as arguments precisely because the right value is a
property of the study, not of the method. Report the numbers, not only the
verdict.
}
\examples{
audit_control(n_perm = 199, quadratic = FALSE)
}
\references{
Austin, P. C. (2011). An introduction to propensity score methods for
reducing the effects of confounding in observational studies.
\emph{Multivariate Behavioral Research}, 46(3), 399-424.
}
