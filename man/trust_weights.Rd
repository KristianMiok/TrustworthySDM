% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{trust_weights}
\alias{trust_weights}
\title{Trust weights: soft downweighting instead of a hard cut}
\usage{
trust_weights(x, floor = 0.05)
}
\arguments{
\item{x}{An \code{sdm_audit} object from \code{\link[=audit]{audit()}}.}

\item{floor}{Minimum weight; prevents low-propensity records from being
dropped in all but name.}
}
\value{
A numeric vector of weights, one per audited record.
}
\description{
Trust weighting keeps \strong{every} record but downweights those the propensity
model considers likely to be mislocated, with
\eqn{w = 1 - P(r = 0 \mid e)}, clipped at a floor so that no record is
annihilated. It is the continuous relaxation of hard filtering, and it moves
the niche in the \strong{same} direction as filtering, not the opposite one. It is
therefore not an alternative to \code{\link[=ipw_weights]{ipw_weights()}}; it sits on the same end of
the bracket as the filter.
}
\examples{
d <- sim_occ(n = 400, seed = 1)
a <- audit(d,
  quality = "keep_precision", env = c("bio1", "bio12", "elev"),
  control = audit_control(n_perm = 99, max_n = 200)
)
w <- trust_weights(a)
range(w)
}
\seealso{
\code{\link[=ipw_weights]{ipw_weights()}}
}
