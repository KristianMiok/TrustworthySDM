% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_occ}
\alias{sim_occ}
\title{Simulated occurrence records with three different quality filters}
\usage{
sim_occ(
  n = 800,
  n_basin = 20L,
  beta = 1.2,
  outlier_prop = 0.1,
  cert_cut = 0.6,
  leak = 0.02,
  seed = NULL
)
}
\arguments{
\item{n}{Number of records.}

\item{n_basin}{Number of basins (used as the blocking variable).}

\item{beta}{Strength of the coupling between coordinate precision and
elevation. \code{beta = 0} decouples quality from the environment entirely and
makes \code{keep_precision} benign as well; raising it strengthens the shift.}

\item{outlier_prop}{Proportion of records removed by \code{keep_outlier}.}

\item{cert_cut}{Elevation (in standardised units) above which \code{keep_strict}
almost always certifies a record.}

\item{leak}{Probability that \code{keep_strict} certifies a record below
\code{cert_cut} anyway. Small values make positivity fail; \code{leak = 0} empties
those environments completely and no weighting can recover them.}

\item{seed}{Optional RNG seed.}
}
\value{
A data frame with environmental columns (\code{bio1}, \code{bio12}, \code{elev}),
metadata (\code{year}, \code{source}, \code{coord_uncertainty}), a blocking column
(\code{basin}), coordinates (\code{x}, \code{y}), and the three retention columns.
}
\description{
A small, self-contained data set for examples, tests and teaching. It is a
simulation, not data: no real occurrences are shipped with this package.
}
\details{
Records sit in \code{n_basin} river basins. Elevation carries a basin-level random
effect; temperature and precipitation are functions of elevation plus noise,
which is what makes the environment spatially structured in the way that
matters. Coordinate uncertainty is generated to depend on elevation, so that
record \emph{quality} and the \emph{environment} are coupled through terrain, exactly
as they are in real georeferenced data.

Three retention columns are supplied, and they are the point of the object.
Each is a filter, and each should receive a different verdict from \code{\link[=audit]{audit()}}:

\describe{
\item{\code{keep_random}}{Records dropped completely at random. The audit should
call this \strong{benign}, and if it does not, the audit is firing
indiscriminately.}
\item{\code{keep_precision}}{Records dropped for imprecise coordinates, where
imprecision tracks elevation. This is the ordinary case: a filter defined
on metadata that projects onto the environment. The audit should detect a
shift.}
\item{\code{keep_outlier}}{Records dropped for being environmental outliers, in
the manner of a Mahalanobis-distance outlier screen. This filter is
symmetric: it trims both tails, so it moves no mean and produces
\code{smd} near zero. It is nonetheless severe, because it deflates the niche
and drives the retention probability to zero at the environmental margins.
It is here as a standing counter-example to any audit that looks only at
mean shifts.}
\item{\code{keep_strict}}{A strict precision cut, leaking a subset of modern
records that are precise whatever the terrain. The leaked records sit
where the environment says retention is unlikely, so they carry enormous
inverse-propensity weights and the effective sample size collapses. The
audit should return \strong{positivity-limited}: the shift is real, and
reweighting cannot be trusted to undo it.}
}
}
\examples{
d <- sim_occ(n = 300, seed = 1)
str(d)
colMeans(d[, c("keep_random", "keep_precision", "keep_outlier")])
}
